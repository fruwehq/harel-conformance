id: deephist
events:
  sub: {}
  pause: {}
  resume: {}
top:
  initial: { transition_to: work }
  states:
    work:
      type: composite
      history: deep
      initial: { transition_to: outer }
      on_events:
        pause: { transition_to: paused }
      states:
        outer:
          type: composite
          initial: { transition_to: s1 }
          states:
            s1:
              on_events:
                sub: { transition_to: s2 }
            s2: {}
    paused:
      on_events:
        resume: { transition_to: work }   # deep history restores the full nested config
