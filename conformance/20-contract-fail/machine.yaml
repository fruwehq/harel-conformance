id: badprov
contracts: [prov_contract]
events:
  configure: {}
top:
  initial: { transition_to: unconfigured }
  states:
    unconfigured:
      on_events:
        configure: { transition_to: ready }
    ready: {}            # never handles `make`, never spawns `worker`
