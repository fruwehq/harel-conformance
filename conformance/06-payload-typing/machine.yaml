id: payloads
events:
  set: { payload: { n: { type: int, required: true } } }
top:
  esvs:
    n: { type: int, init: 0 }
  initial: { transition_to: s }
  states:
    s:
      on_events:
        set:
          action: [ { assign: { n: "event.payload.n" } } ]   # internal
