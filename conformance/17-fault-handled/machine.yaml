id: faulty
events:
  boom: {}
top:
  esvs:
    x: { type: int, init: 7 }
  on_events:
    error: { transition_to: recovered }   # reserved fault event, handled here
  initial: { transition_to: ok }
  states:
    ok:
      on_events:
        boom:
          action: [ { assign: { x: "x / 0" } } ]   # division by zero -> CEL runtime error
    recovered: {}
