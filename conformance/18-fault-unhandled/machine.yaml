id: faultyun
events:
  boom: {}
top:
  esvs:
    x: { type: int, init: 7 }
  initial: { transition_to: ok }       # no `error` handler anywhere
  states:
    ok:
      on_events:
        boom:
          action: [ { assign: { x: "x / 0" } } ]   # division by zero -> CEL runtime error
