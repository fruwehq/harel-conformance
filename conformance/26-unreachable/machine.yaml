id: unreachable
events: { go: {} }
top:
  initial: { transition_to: a }
  states:
    a: { on_events: { go: { transition_to: b } } }
    b: {}
    orphan: {}          # nothing targets it -> unreachable
