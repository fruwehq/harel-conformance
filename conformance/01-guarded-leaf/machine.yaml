id: gate
events:
  enter: { payload: { code: { type: string } } }
top:
  esvs:
    secret: { type: string, init: "open-sesame" }
  initial: { transition_to: locked }
  states:
    locked:
      on_events:
        enter: { transition_to: open, guard: "event.payload.code == secret" }
    open: {}
